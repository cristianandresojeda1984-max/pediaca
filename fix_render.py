"""
fix_render.py — OBSOLETO.

Era un parche manual equivalente a setup_db.py. Superado por
`_init_configuraciones()` en app.py e `init_db.py`. Se deja como no-op.
"""
print("⚠️  fix_render.py está obsoleto y ya no hace falta. "
      "La tabla 'configuraciones' se crea sola al arrancar la app.")
